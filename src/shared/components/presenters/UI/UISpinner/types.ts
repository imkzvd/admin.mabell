import type { UIColor } from '@/shared/components/presenters/UI/types.ts';

export type UISpinnerProps = {
  size?: string | number;
  width?: string | number;
  color?: UIColor;
  isCentered?: boolean;
};
